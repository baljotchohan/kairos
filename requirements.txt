# API
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-dotenv==1.0.1
websockets==13.1
pydantic>=2.10.1
python-multipart==0.0.12
firebase-admin==6.5.0

# AI — Fireworks AI (AMD hardware, OpenAI-compatible client)
openai==1.51.0

# Memory
chromadb==0.5.20
networkx==3.4.2

# Agents / Orchestration
langgraph==0.2.55
langchain-core==0.3.21

# Connectors
slack-sdk==3.33.4
slack-bolt>=1.21.0
google-auth==2.36.0
google-auth-oauthlib==1.2.1
google-auth-httplib2==0.2.0
google-api-python-client==2.154.0
requests==2.32.3
httpx==0.27.2

# Meeting transcription (optional — local Whisper pulls ~2-3GB PyTorch).
# meeting_agent.py degrades gracefully when these are absent, so they are
# left out of the default/HF deploy to keep the image small and the build fast.
# Install locally with: pip install openai-whisper ffmpeg-python
# openai-whisper
# ffmpeg-python==0.2.0

# MCP server
mcp==1.2.0

# Utilities
aiofiles==24.1.0
